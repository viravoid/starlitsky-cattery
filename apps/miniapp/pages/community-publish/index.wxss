.page {
  min-height: 100vh;
  padding: 32rpx 40rpx calc(166rpx + env(safe-area-inset-bottom));
  background: var(--xy-color-background);
}

.state {
  padding: 120rpx 20rpx;
  color: var(--xy-color-muted);
  font-size: 26rpx;
  text-align: center;
}

.error {
  color: #b42318;
}

.error-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 22rpx;
}

.header {
  display: none;
}

.title {
  display: block;
  color: var(--xy-color-heading);
  font-size: 42rpx;
  font-weight: 700;
}

.subtitle {
  display: block;
  margin-top: 8rpx;
  color: var(--xy-color-muted);
  font-size: 24rpx;
}

.retry-button,
.submit-button {
  border-radius: 999rpx;
  background: var(--xy-color-violet);
  color: #ffffff;
  font-size: 26rpx;
  font-weight: 700;
}

.retry-button {
  min-width: 176rpx;
  padding: 0 28rpx;
  line-height: 72rpx;
}

.retry-button::after,
.option::after,
.add-image::after,
.remove-image::after,
.restore-image::after,
.submit-button::after {
  border: 0;
}

.form {
  display: flex;
  flex-direction: column;
  gap: 32rpx;
}

.section {
  padding: 0;
}

.form > .section:nth-of-type(2) {
  order: 1;
}

.form > .section:nth-of-type(3) {
  order: 2;
}

.form > .section:nth-of-type(6) {
  order: 3;
}

.form > .section:nth-of-type(4) {
  order: 4;
}

.form > .section:nth-of-type(5) {
  order: 5;
}

.form > .notice {
  order: 6;
}

.form > .bottom-cta {
  order: 7;
}

.label {
  display: block;
  margin-bottom: 16rpx;
  color: var(--xy-color-heading);
  font-size: 26rpx;
  font-weight: 700;
}

.option-row {
  display: flex;
  flex-wrap: wrap;
  gap: 14rpx;
}

.option {
  min-height: 62rpx;
  padding: 0 22rpx;
  border: 1rpx solid var(--xy-color-border);
  border-radius: 999rpx;
  background: #ffffff;
  color: #6b8db3;
  font-size: 24rpx;
  font-weight: 700;
  line-height: 62rpx;
}

.option.active {
  border-color: var(--xy-color-accent);
  background: #f9f0d4;
  color: var(--xy-color-accent-text);
}

.content-input {
  width: 100%;
  min-height: 280rpx;
  box-sizing: border-box;
  padding: 24rpx 32rpx;
  border: 1rpx solid var(--xy-color-border);
  border-radius: 32rpx;
  background: var(--xy-color-background);
  color: var(--xy-color-text);
  font-size: 27rpx;
  line-height: 1.65;
}

.empty-box {
  padding: 30rpx;
  border-radius: 28rpx;
  background: var(--xy-color-background);
  color: var(--xy-color-muted);
  font-size: 24rpx;
  text-align: center;
}

.image-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 14rpx;
}

.image-hint {
  display: block;
  margin-top: 12rpx;
  color: var(--xy-color-accent-text);
  font-size: 22rpx;
}

.image-wrap {
  position: relative;
}

.image-removed .image-tile {
  opacity: 0.34;
}

.image-tile,
.add-image {
  width: 100%;
  height: 208rpx;
  border-radius: 28rpx;
}

.image-tile {
  background: var(--xy-gradient-cream);
}

.add-image {
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2rpx dashed var(--xy-color-border);
  background: #ffffff;
  line-height: 208rpx;
}

.add-image-icon {
  width: 48rpx;
  height: 48rpx;
}

.remove-image {
  position: absolute;
  right: 8rpx;
  top: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  width: 42rpx;
  height: 42rpx;
  border-radius: 999rpx;
  background: rgba(65, 84, 108, 0.82);
  line-height: 42rpx;
}

.remove-image-icon {
  width: 24rpx;
  height: 24rpx;
}

.restore-image {
  position: absolute;
  left: 50%;
  top: 50%;
  min-width: 86rpx;
  height: 46rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.92);
  color: var(--xy-color-heading);
  font-size: 24rpx;
  line-height: 46rpx;
  transform: translate(-50%, -50%);
}

.bottom-cta {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 4;
  padding: 20rpx 40rpx calc(20rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--xy-color-border);
  background: rgba(255, 253, 248, 0.96);
}

.notice {
  color: var(--xy-color-accent-text);
  font-size: 22rpx;
  line-height: 1.7;
}

.notice-pill {
  display: inline-flex;
  margin-bottom: 8rpx;
  padding: 4rpx 14rpx;
  border-radius: 999rpx;
  background: rgba(180, 135, 37, 0.35);
  color: var(--xy-color-accent-text);
  font-weight: 700;
}

.notice-text {
  display: block;
}

.submit-button {
  width: 100%;
  line-height: 88rpx;
}

.submit-button[disabled] {
  background: #c6cbd2;
}
